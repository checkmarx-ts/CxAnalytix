name: build-ci
on: push
jobs:
    example:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2
            - name: Dump Environment
              run: |
                set
                pwd
                ls -l
              
              
            
