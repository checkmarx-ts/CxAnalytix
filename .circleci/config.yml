version: 2.1
jobs:
  test:
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk
    steps:
      - checkout
      - run: set
      - run: dotnet test CxAnalytix.sln
  build:
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk
    steps:
      - checkout
      - run: dotnet publish CxAnalytix.sln -c Release -o artifacts
      
